<section>
    <%= ( typeof title != 'undefined' ) ? title : '' %>
    
    <% results1.forEach(result => { %>
        <section>
            <h2><%= result.title %></h2>
            <p><%= result.body_text %></p>
        </section>
        <hr>
    <% }); %>
</section>